import { ContactUsComponent } from "@/components/ui/animated-characters-login-page";

export default function Contact() {
  return (
    <section id="contact" className="w-full">
      <ContactUsComponent />
    </section>
  );
}
